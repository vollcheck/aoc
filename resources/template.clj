(ns yYEAR.dayDAY
  (:require [clojure.string :as str]
            [core :refer [load-in]]))

(defn part-1 [in]
  )

(defn part-2 [in]
  )

(comment
  (def test-input (load-in :test))
  (def input (load-in))
  (part-1 test-input)
  (part-1 input)
  (part-2 test-input)
  (part-2 input)
  )
